apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: email-worker
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: sre-platform

spec:
  project: default

  source:
    repoURL: https://github.com/MekusB/sre-proj-upgrade
    targetRevision: main
    path: kustomize/overlays/dev/email-worker

  destination:
    server: https://kubernetes.default.svc
    namespace: workers

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
